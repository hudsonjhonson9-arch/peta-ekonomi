# Task 3 Report: ui.jsx — formatBytes, extractGDriveFolderId, embedded folder preview

## What I implemented

Modified `src/components/ui.jsx` exactly per the brief:

1. **Added `extractGDriveFolderId`** — extracts folder ID from `/folders/<id>` URLs, placed AFTER `extractGDriveFileId`, BEFORE `isGDriveUrl`. Verbatim from brief.
2. **Added `formatBytes`** — returns `—` for falsy/zero, `X.X MB` above 1 MiB (1 decimal), `X KB` otherwise (0 decimals). Matches GAS size format used elsewhere. Placed AFTER `isGDriveUrl`, BEFORE `GoogleDriveEmbed`. Verbatim from brief.
3. **Modified `GoogleDriveEmbed`** — resolves `folderId` via `extractGDriveFolderId` and `fileId` via `extractGDriveFileId`; returns null only when both absent; picks `https://drive.google.com/embeddedfolderview?id=<folderId>#list` when a folder, else the existing file `/preview` URL. Verbatim from brief.

No other components touched. No dependencies added. Named-export and EM-dash style preserved.

## Verification

Command: `npm run build` (from `D:\Code\peta-ekonomi`, PowerShell)

Output:
```
vite v5.4.21 building for production...
✓ 92 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                1.33 kB │ gzip: 0.65 kB
dist/assets/index-BcFrFRBy.js  313.25 kB │ gzip: 84.16 kB
✓ built in 1.19s
```

Exit code: 0. No errors.

## Files changed

- `src/components/ui.jsx` (+18 / -2)
- `.superpowers/sdd/task-3-report.md` (this report)

## Commit

`57d0427` — `feat: dukung preview folder Drive (embeddedfolderview) + formatBytes`

Only `src/components/ui.jsx` staged. Not pushed.

## Self-review

- Helpers placed at the exact positions the brief specified (after `extractGDriveFileId`, after `isGDriveUrl`).
- `GoogleDriveEmbed` behavior matches brief verbatim; file URLs unchanged, folder URLs render embedded folder list.
- `formatBytes` handles falsy/0 (returns `—`), `1 MiB` boundary (strict `>` so exactly 1 MiB shows as `1024 KB` — matches GAS convention in the brief's own code).
- Build passes.

## Concerns

None. Note: `dist/` artifacts are generated by the build; not committed (out of scope). Untracked `.superpowers/` and `docs/superpowers/` dirs belong to the planning workflow, left as-is.